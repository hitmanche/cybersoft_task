namespace CL.Models
{
    public enum AliveType
    {
        Enemy = 1,
        Human = 2,
        Distance=3,
        None=4
    }

    public enum ReadType
    {
        XML=1,
        TXT=2
    }

    public enum WriteType
    {
        DOCX=1,
        TXT=2
    }
}
